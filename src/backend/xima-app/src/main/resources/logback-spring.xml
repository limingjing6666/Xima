<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 定义启动时间戳，用于日志文件名 -->
    <timestamp key="startupTime" datePattern="yyyy-MM-dd_HH-mm-ss"/>
    
    <!-- 日志存放路径：优先使用环境变量，否则使用默认值 -->
    <property name="LOG_PATH" value="${LOG_DIR:-./logs}"/>
    
    <!-- 控制台输出（Windows 终端使用 GBK 编码） -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- 文件输出 - 每次启动创建新文件 -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_PATH}/xima-app_${startupTime}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- 应用日志级别 -->
    <logger name="com.xima" level="DEBUG"/>
    <logger name="com.xima.app.mapper" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
    
    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
